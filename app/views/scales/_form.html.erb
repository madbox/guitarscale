<%= form_for(@scale) do |f| %>
  <% if @scale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scale.errors.count, "error") %> prohibited this scale from being saved:</h2>

      <ul>
      <% @scale.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <b><%= f.label :name %></b> <%= f.text_field :name %>
  </div>

  <div class="field">
    <b><%= f.label :intervals_string %></b> <%= f.text_field :intervals_string %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
